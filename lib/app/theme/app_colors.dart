import 'package:flutter/material.dart';

class AppColors {
  // A restrained, slightly warm palette inspired by aged paper and charcoal
  static const background = Color(0xFFF7F5F0); // warm paper
  static const surface = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF171717); // near-black
  static const textSecondary = Color(0xFF6B6B6B);
  static const border = Color(0xFFD8D5CE);
  static const muted = Color(0xFF8A8A8A);
  // Accent: muted Japanese vermilion
  static const accent = Color(0xFFA63D2F);
  // Charcoal for headings and UI elements
  static const charcoal = Color(0xFF222222);
}
